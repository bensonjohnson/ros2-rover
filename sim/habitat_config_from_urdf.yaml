# Habitat configuration generated from tractor.urdf.xacro
# Robot dimensions extracted from URDF

SIMULATOR:
  AGENT_0:
    HEIGHT: 0.0900  # 90.0mm (track height)
    RADIUS: 0.0930  # 93.0mm (half of total width including tracks)

  # RGB Camera (RealSense D435i)
  RGB_SENSOR:
    WIDTH: 640
    HEIGHT: 480
    HFOV: 69  # Horizontal FOV in degrees
    POSITION: [0, 0.1230, 0]  # [x, y, z] in meters - camera height from ground
    ORIENTATION: [0, 0, 0]  # [roll, pitch, yaw]

  # Depth Camera (RealSense D435i)
  DEPTH_SENSOR:
    WIDTH: 640
    HEIGHT: 480
    HFOV: 69
    MIN_DEPTH: 0.0
    MAX_DEPTH: 10.0
    POSITION: [0, 0.1230, 0]
    ORIENTATION: [0, 0, 0]

# Physical properties for reference
# Total mass: 9.0 kg
# Base dimensions: 266.7mm × 186.0mm × 90.0mm
# Wheel separation: 144.0mm
# Wheel radius: 48.5mm
